name: Java CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Build with Maven
      run: mvn -B package --file pom.xml org.sonarsource.scanner.maven:sonar -Dsonar.projectKey=huynhqtienvtag_ChestCommands -Dsonar.organization=huynhqtienvtag-github -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=787a64d64e916c33a837d5d15e9a7a215709ae1c